import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.homescriptone.chillradio',
  appName: 'chillradio',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
